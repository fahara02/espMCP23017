file(GLOB_RECURSE SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp ${CMAKE_CURRENT_SOURCE_DIR}/*.c)

add_library(espMCP23017 STATIC ${SOURCES})

target_include_directories(espMCP23017 PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
